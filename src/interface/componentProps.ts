import React from "react";

export interface IHeaderProps {
    children: React.ReactNode;
    margin?: string,
    color?: string
    fontSize?: string,
    lineHeight?: string,
    maxWidth?: string,
    media?: string
}